# 2026-09-23

## 코스명 자동 제안 주소 중복 수정

### 한 일

- 북한강 코스의 자동 제안명이 `화천군 강원특별자치도 화천군 …`처럼 시·군과 전체 주소를 중복 표시하는 문제를 수정했다.
- 검색 지점 라벨이 `시·군 - 전체 주소` 형식이면 뒤쪽 전체 주소를 기준으로 도·특별자치도만 제거한다.
- 역지오코딩 주소도 시·군·구부터 읍·면·동·리와 번지까지 유지한다.
- 결과 예시는 `북한강 - 화천군 간동면 구만리 1395-1 ~ 춘천시 서면 오월리 51-2`다.

### 검증·배포

- 주소 변환 단위 테스트를 문자열 존재 검사에서 실제 JavaScript 함수 실행 방식으로 바꾸고 화천·춘천·서울 사례를 추가했다.
- Python 회귀 테스트 22건과 코스명 Playwright 브라우저 테스트를 통과했다.
- `index.html`, `map.html`, `test.html`, `tour/index.html`을 다시 빌드했다.
- 커밋 `68df967`을 `main`에 푸시하고 `https://canoe.crowdbase.kr/`에서 새 주소 로직을 확인했다.

## 수정 후 자동 배포 정착

### 한 일

- 사용자가 로컬 수정이나 배포 금지를 명시하지 않은 서비스 변경은 검증, 작업 파일 선별 커밋, `main` 푸시,
  투어 배포, 공개 URL 확인까지 완료 조건으로 삼도록 프로젝트 규칙을 갱신했다.
- `.github/workflows/deploy-tour.yml`을 추가했다. `main`의 투어 관련 파일이 바뀌면 Python 회귀 테스트를 실행하고
  필요한 정적 자원만 묶어 Cloudflare Pages의 `mycanoe-tour` 프로젝트에 배포한다.
- GitHub 저장소 Secret `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`을 연결했다.

### 검증·배포

- 워크플로 YAML 구문과 Python 회귀 테스트 22건을 확인했다.
- 커밋 `49034fb`을 `main`에 푸시했다. 첫 실행은 Cloudflare Secret 부재로 실패했으나 Secret 등록 후 실패 작업을
  재실행해 GitHub Actions `35816863592`가 성공했다.
- `https://canoe.crowdbase.kr/`와 `https://tour.crowdbase.kr/` 양쪽에서 새 주소 변환 코드가 제공되는 것을 확인했다.

### 다음 시작점

- 이후 서비스 코드·데이터 수정은 같은 자동 배포 흐름을 사용하고, 배포 성공과 공개 URL 확인 결과를 당일 저널에 남긴다.
- 기존 공간객체 통합 설계·기준선 작업의 미커밋 변경은 이번 두 커밋에 섞지 않고 그대로 보존했다.
