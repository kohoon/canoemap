# 2026-09-14

## 한 일

- 메인 지도와 투어 지도에 `📥 오프라인` 컨트롤, 저장 상태·업데이트·삭제·간이 지도 전환 모달을 추가했다.
- 코스 상세에서도 선택 코스를 바로 오프라인 저장할 수 있게 했다. 코스가 없으면 현재 지도 화면을 저장 범위로 사용한다.
- 코스/화면 외곽에 최소 약 2km 여유를 두고 전국 하천·도로 GeoJSON을 잘라 IndexedDB에 저장한다.
- 상수원보호·수상레저금지·물놀이 관리지역은 CacheStorage에 넣어 기존 줌게이트 로더가 오프라인에서도 읽게 했다.
- 저장 팩과 함께 선택 코스, 범위 내 지형지물, 로그인 후 이미 받은 런칭·랜딩을 보관한다. 런칭·랜딩은 팩 저장 사용자와
  현재 로그인 사용자가 같을 때만 복원하며 인증 토큰은 저장하지 않는다.
- 통신 단절을 감지하면 일반/위성 타일을 제거하고 저장된 간이 하천·도로 베이스로 자동 전환하며 상단 상태 배너를 표시한다.
- PWA manifest, 아이콘, Service Worker를 추가했다. 앱 셸은 HTML·Leaflet 자원을 저장해 오프라인 새로고침을 지원한다.
- 투어 종료 저장이 네트워크 오류로 실패하면 기록을 휴대폰 대기열에 보관하고 온라인 복귀 시 자동 전송하게 했다.
- 트립과 자유 투어 파생 코스에 `clientId` 멱등 처리를 넣어 재전송이 중복 기록으로 이어지지 않게 했다.
- 모달에 준비부터 비행기 모드 사전 점검까지 4단계 사용법과 GPS·실시간 자료 한계를 함께 표시했다.
- 오프라인 팩에 Esri World Imagery 위성영상을 추가했다. 코스가 있으면 물길 좌우 약 2km, 없으면 현재 화면을 저장하며,
  줌 10~16 범위에서 최대 800장을 넘지 않도록 최고 줌을 자동으로 낮춘다.
- 저장 전 예상 용량과 타일 수, 저장 중 받은 타일 수·실제 용량, 저장 후 총 용량을 표시한다. VWorld 위성은 저장하지 않는다.

## 결정·근거

- OpenStreetMap 표준 타일 서버는 대량 다운로드·오프라인 선저장을 허용하지 않으므로 래스터 타일을 저장하지 않는다.
  프로젝트가 이미 배포하는 OSM 기반 하천·도로 벡터를 범위별 간이 배경으로 재사용한다.
- 전국 도로 원본은 6MB 이상이지만 코스와 교차하는 선분만 잘라 IndexedDB에 저장한다. 법규·안전 GeoJSON은 기존
  팝업과 스타일을 그대로 재사용하기 위해 CacheStorage에 원본 응답을 보관한다.
- 브라우저의 `navigator.onLine`만으로 통신 가능 여부를 단정하지 않고, Service Worker가 캐시하지 않는 동일출처
  probe도 확인한다. 식별자만으로 로그인 활성 상태를 판단하지 않고 현재 로그인 토큰은 서버 전송 시 다시 검증한다.
- 오프라인 투어 대기열에는 토큰을 기록하지 않는다. 서버는 동일 사용자·동일 `clientId`를 확인해 이미 저장된 현재
  기록이면 성공으로 응답하고 집계·피드를 다시 늘리지 않는다.
- Esri 타일은 새 팩 전용 CacheStorage에 먼저 모두 받은 뒤 IndexedDB 정본을 교체하고, 성공 후에만 이전 캐시를 지운다.
  저장 실패 시 불완전한 새 캐시만 삭제해 기존 오프라인 팩을 유지한다. Esri의 공식 오프라인 방식은 인증된 타일 내보내기
  서비스이므로 현재 브라우저 캐시 방식이 공급자 정책·계정 조건에 따라 제한될 수 있음을 모달에 명시했다.
- 재열기 중 빈 오프라인 레이어를 먼저 선택해 일반지도로 되돌리던 초기화 경합을 발견했다. 저장된 베이스맵 선택은
  IndexedDB 팩을 읽은 뒤로 미뤄, 네트워크 상태 이벤트가 늦어도 저장 지도가 유지되게 고쳤다.

## 검증

- `python3 tools/build_map.py`, `python3 tools/build_map.py test`와 생성된 메인/투어 HTML의 인라인 JavaScript
  `node --check`를 통과했다.
- `tests/test_offline_mode.py`에서 PWA 파일/안내, OSM 표준 타일 미저장, 동일 사용자만 보호 장소 복원,
  토큰 없는 대기열, 트립·코스 멱등 처리를 점검했다.
- 기존 로드뷰 제공처, 물놀이 좌표/안전요원, 내수면 수상레저 금지구역 회귀 테스트를 통과했다.
- Playwright 모바일 390×844 검증에서 테스트 코스의 Esri 위성 타일 538장(약 5.7MB)이 전부 CacheStorage에 들어가고
  위성 TileLayer가 구성됨을 확인했다. 브라우저 네트워크를 차단한 뒤 새로고침해도 `오프라인 지도 사용 중` 배너,
  공유 코스와 실제 로드된 위성 타일이 복원됐다. JavaScript 페이지 오류는 0건이었다.

## 미해결·다음 시작점

- 실제 iPhone Safari와 Android Chrome에서 홈 화면 설치, 저장공간 제거 조건, 장시간 전경 GPS 기록을 현장 검증해야 한다.
- Esri 위성영상에는 오프라인 지명 라벨이 없다. 현장 피드백에서 지명이 필요하면 오프라인 사용을 명시적으로 허용하는
  벡터 타일 제공처 또는 자체 PMTiles 생성·배포를 다음 단계로 검토한다.
